# nixos-cache-signing-server

> **NOTE**: The [`secret-key`][./secret-key] and [`pubkey`][./pub] are committed for ease of testing.
> Don't trust them since they're, well, public.
> You can generate your own if you so choose.
